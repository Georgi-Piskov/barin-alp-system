{
  "name": "BARIN-ALP: Parse Bank Statement CSV",
  "nodes": [
    {
      "parameters": {
        "httpMethod": "POST",
        "path": "barin-alp/bank-statement",
        "responseMode": "responseNode",
        "options": {
          "allowedOrigins": "*"
        }
      },
      "id": "webhook",
      "name": "Webhook",
      "type": "n8n-nodes-base.webhook",
      "typeVersion": 2,
      "position": [250, 300],
      "webhookId": "barin-alp-bank-statement"
    },
    {
      "parameters": {
        "jsCode": "// Get the CSV content (text)\nconst body = $input.item.json.body;\nconst csvContent = body.csv;\n\nif (!csvContent) {\n  return [{ json: { success: false, error: 'No CSV content provided' } }];\n}\n\n// Return the CSV text for parsing\nreturn [{ json: { csvContent } }];"
      },
      "id": "prepare",
      "name": "Prepare CSV",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [450, 300]
    },
    {
      "parameters": {
        "jsCode": "// Parse Asset Bank CSV format with overdraft detection and fee aggregation\n// Columns (separated by ;):\n// 0: Сметка/Карта (IBAN)\n// 1: Дата (DD.MM.YYYY)\n// 2: Дб/Кр\n// 3: Сума\n// 4: Валута\n// 5-6: BIC/IBAN на наредител\n// 7-8: Тип/Име на наредител\n// 9-10: BIC/IBAN на получател\n// 11-12: Тип/Име на получател\n// 13: Тип операция\n// 14-15: Основание 1/2\n// 17: Референция\n\nconst csvContent = $input.item.json.csvContent;\nconst lines = csvContent.split('\\n');\n\nconst rawTransactions = [];\nconst processedRefs = new Set();\n\n// Fee threshold for aggregation\nconst FEE_THRESHOLD = 10; // euros\nconst FEE_KEYWORD = 'автоматична такса';\n\n// Skip header row, parse all transactions first\nfor (let i = 1; i < lines.length; i++) {\n  const line = lines[i].trim();\n  if (!line) continue;\n  \n  const cols = line.split(';');\n  if (cols.length < 14) continue;\n  \n  const iban = cols[0].trim();\n  const dateStr = cols[1].trim();\n  const typeRaw = cols[2].trim().toLowerCase();\n  const amountStr = cols[3].trim();\n  const currency = cols[4].trim();\n  const senderName = cols[8] ? cols[8].trim() : '';\n  const receiverName = cols[12] ? cols[12].trim() : '';\n  const operationType = cols[13] ? cols[13].trim() : '';\n  const purpose1 = cols[14] ? cols[14].trim() : '';\n  const purpose2 = cols[15] ? cols[15].trim() : '';\n  const reference = cols[17] ? cols[17].trim() : '';\n  \n  // Skip if not main IBAN\n  if (!iban.startsWith('BG')) continue;\n  \n  // Skip duplicates by reference\n  if (reference && processedRefs.has(reference)) continue;\n  if (reference) processedRefs.add(reference);\n  \n  // Parse date\n  const dateParts = dateStr.split('.');\n  const isoDate = dateParts.length === 3 \n    ? `${dateParts[2]}-${dateParts[1]}-${dateParts[0]}`\n    : dateStr;\n  \n  // Parse amount\n  const cleanAmount = amountStr.replace(/\\s/g, '').replace(',', '.');\n  const amount = parseFloat(cleanAmount) || 0;\n  \n  // Determine debit/credit\n  const isDebit = typeRaw.includes('дебит') || typeRaw.includes('debit');\n  const type = isDebit ? 'debit' : 'credit';\n  \n  rawTransactions.push({\n    rowIndex: i,\n    date: isoDate,\n    reference,\n    description: operationType || purpose1,\n    operationType: operationType.toLowerCase(),\n    type,\n    amount,\n    currency: currency || 'EUR',\n    iban,\n    senderName,\n    receiverName,\n    purpose: (purpose1 + ' ' + purpose2).trim(),\n    purpose1,\n    purpose2\n  });\n}\n\n// ========== STEP 1: Detect and remove overdraft duplications ==========\n// Overdraft creates pairs: same amount, consecutive rows, one credit + one debit\nconst overdraftIndices = new Set();\n\nfor (let i = 0; i < rawTransactions.length - 1; i++) {\n  const curr = rawTransactions[i];\n  const next = rawTransactions[i + 1];\n  \n  // Check if same amount, different types (credit/debit pair)\n  if (Math.abs(curr.amount - next.amount) < 0.01) {\n    const isOverdraftPair = \n      (curr.type === 'credit' && next.type === 'debit') ||\n      (curr.type === 'debit' && next.type === 'credit');\n    \n    if (isOverdraftPair) {\n      // Mark the credit one for removal, keep only the debit (expense)\n      if (curr.type === 'credit') {\n        overdraftIndices.add(i);\n      } else {\n        overdraftIndices.add(i + 1);\n      }\n    }\n  }\n}\n\n// Filter out overdraft credits\nconst filteredTransactions = rawTransactions.filter((_, idx) => !overdraftIndices.has(idx));\n\n// ========== STEP 2: Aggregate small fees (\"Автоматична такса\" <= 10 EUR) ==========\nconst finalTransactions = [];\nlet aggregatedFees = 0;\nlet feeCount = 0;\nlet firstFeeDate = null;\nlet lastFeeDate = null;\n\nfilteredTransactions.forEach(tx => {\n  const isFee = tx.operationType.includes(FEE_KEYWORD.toLowerCase()) && tx.amount <= FEE_THRESHOLD && tx.type === 'debit';\n  \n  if (isFee) {\n    aggregatedFees += tx.amount;\n    feeCount++;\n    if (!firstFeeDate || tx.date < firstFeeDate) firstFeeDate = tx.date;\n    if (!lastFeeDate || tx.date > lastFeeDate) lastFeeDate = tx.date;\n  } else {\n    // Regular transaction - add as is, without categories\n    let displayName = tx.description || tx.purpose1;\n    let counterpartyName = tx.type === 'debit' ? tx.receiverName : tx.senderName;\n    \n    // Extract invoice reference if present\n    let invoiceRef = '';\n    const invoiceMatch = tx.purpose.match(/(?:фактура|ф-ра|invoice|inv)[\\s#:]*([\\d\\/]+)/i);\n    if (invoiceMatch) invoiceRef = invoiceMatch[1];\n    \n    finalTransactions.push({\n      date: tx.date,\n      reference: tx.reference,\n      description: tx.description,\n      displayName: counterpartyName || displayName,\n      type: tx.type,\n      amount: tx.amount,\n      currency: tx.currency,\n      iban: tx.iban,\n      counterpartyName,\n      invoiceRef,\n      purpose: tx.purpose,\n      objectId: null,\n      objectName: null,\n      status: 'unmatched'\n    });\n  }\n});\n\n// Add aggregated fees as single transaction if any\nif (aggregatedFees > 0) {\n  finalTransactions.push({\n    date: lastFeeDate || new Date().toISOString().split('T')[0],\n    reference: 'FEES-AGGREGATED-' + (lastFeeDate || '').replace(/-/g, ''),\n    description: `Банкови такси (${feeCount} бр.)`,\n    displayName: `Банкови такси (${feeCount} бр.)`,\n    type: 'debit',\n    amount: Math.round(aggregatedFees * 100) / 100,\n    currency: 'EUR',\n    iban: filteredTransactions[0]?.iban || '',\n    counterpartyName: 'Банка',\n    invoiceRef: '',\n    purpose: `Обединени банкови такси за периода ${firstFeeDate} - ${lastFeeDate}`,\n    objectId: null,\n    objectName: null,\n    status: 'unmatched',\n    isAggregatedFees: true\n  });\n}\n\n// Sort by date descending\nfinalTransactions.sort((a, b) => b.date.localeCompare(a.date));\n\n// Calculate totals\nconst totalDebit = finalTransactions.filter(t => t.type === 'debit').reduce((sum, t) => sum + t.amount, 0);\nconst totalCredit = finalTransactions.filter(t => t.type === 'credit').reduce((sum, t) => sum + t.amount, 0);\nconst netChange = Math.round((totalCredit - totalDebit) * 100) / 100;\n\nreturn [{ \n  json: { \n    success: true, \n    data: {\n      transactions: finalTransactions,\n      count: finalTransactions.length,\n      totalDebit: Math.round(totalDebit * 100) / 100,\n      totalCredit: Math.round(totalCredit * 100) / 100,\n      aggregatedFeesTotal: Math.round(aggregatedFees * 100) / 100,\n      aggregatedFeesCount: feeCount,\n      overdraftPairsRemoved: overdraftIndices.size,\n      netChange\n    }\n  } \n}];"
      },
      "id": "parse",
      "name": "Parse Bank Transactions",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [650, 300]
    },
    {
      "parameters": {
        "options": {},
        "respondWith": "json",
        "responseBody": "={{ $json }}"
      },
      "id": "respond",
      "name": "Respond",
      "type": "n8n-nodes-base.respondToWebhook",
      "typeVersion": 1.1,
      "position": [850, 300]
    }
  ],
  "connections": {
    "Webhook": {
      "main": [
        [{ "node": "Prepare CSV", "type": "main", "index": 0 }]
      ]
    },
    "Prepare CSV": {
      "main": [
        [{ "node": "Parse Bank Transactions", "type": "main", "index": 0 }]
      ]
    },
    "Parse Bank Transactions": {
      "main": [
        [{ "node": "Respond", "type": "main", "index": 0 }]
      ]
    }
  },
  "settings": {
    "executionOrder": "v1"
  }
}
